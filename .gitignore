# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.coverage

# Secrets — never commit the Tushare token / endpoint
# 密钥文件及其任何派生形态(.local/.bak/.old 等)——只写 .env.local 挡不住备份文件,
# 实测 .env.local.bak 曾带着 API key 裸奔在未跟踪区。模板文件显式放行(codex 复审)
.env
.env.*
!.env.example
!.env.template

# Data cache (raw Tushare pulls, parquet) — large, regenerable, not for git
# Anchored to repo root so it does NOT swallow the ashare_gauntlet/data/ source package.
# 2026-08-12 用户决定:data/ 与 docs/ 整目录(个人账户状态、研究方法论与实验档案)
# 一律不入公开仓;勿 `git add -f` 加回。本地文件保留,仅移出版本控制。
/data/
/docs/
*.parquet

# Scratch / smoke scripts
_scratch*
_smoke*

# 年报原文文本 / 关键词提取实验残留(非代码,不入库)
_yake_*.txt

# CodeGraph / Cherry Studio 本地状态与会话附件(可重建/可能含个人内容)
.codegraph/
.cherry-studio/

# Claude Code 项目配置与 workflows(本地协作配置,不入公开仓;2026-08-12 用户决定)
/.claude/

# OpenHarness 全目录(含 skills)不入公开仓(2026-08-12 用户决定)
.openharness/

# 协作与工具配置、项目说明(内嵌个人流程/参数描述)不入公开仓(2026-08-12 用户决定)
/.mcp.json
/CLAUDE.md
/README.md

# Understand Anything 默认不启用；生成物可能包含语义化源码摘要
.ua/
.understand-anything/
.codex/
