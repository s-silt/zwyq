# A 股量化与实盘风控项目协作规则

## 职责边界

- 主智能体负责调查、需求澄清、技术设计、任务合同、最终审核、测试与修复。
- OpenHarness 仅按主智能体已经确定的设计实施一个边界明确的代码任务；不得自行改变架构、研究口径或交易政策。
- 同一时间只允许一个编码子助理修改当前工作树。OpenHarness 内不得递归启动其他 agent。
- 子助理完成不等于任务完成；主智能体必须查看实际 diff，不得只采信子助理摘要。

## 环境与工作区

- 项目根目录固定为 `E:\zwyq`。
- Python 固定使用 `E:\zwyq\.venv\Scripts\python.exe`，要求 Python 3.12+；不要调用系统 Python或创建第二套环境。
- 开工前检查 `git status --short --branch`，修改目标文件前检查其现有 diff。
- 当前工作树可能包含用户或其他进程的未提交改动。禁止 `git reset`、`restore`、`checkout --`、`clean`、`stash`，禁止覆盖、丢弃或顺手整理既有改动。
- 只修改任务合同明确列出的路径；目标文件已经修改时，必须由主智能体先理解现有 diff 并明确授权增量修改。
- 禁止无关重构、全仓格式化、整文件换行转换以及修改任务外文件。
- 未经用户明确要求，不得 commit、push、创建 PR 或切换分支。

## 密钥、账户与人工状态

- 禁止读取、打印、记录或提交 `.env*`、Tushare token、API key、Credential Manager 内容、认证 header 和其他秘密。
- 下列文件是真实账户状态或个人档案，智能体不得修改，也不得完整输出其内容：
  - `data/holdings.json`
  - `data/profile.json`
  - `data/trigger_bands.json`
  - `data/trade_journal.json`
- 下列决策类文件允许"确认即代笔"（用户授权 2026-08-15）：智能体先把**将写入的完整内容**
  展示给用户，用户对该具体内容明确确认后，智能体逐字写入并回报校验结果；未经该次确认
  不得写入，也不得把旧确认沿用到新内容。verdict/参数的判断权始终在用户：
  - `data/factcheck_overrides.json`
  - `data/trading_policy.json`
  - `data/holdings.json` 的 **`as_of` 字段**(2026-08-19 追加授权,仅限
    `scripts.holdings_confirm` 这一条推进路径,只改日期不动持仓/现金)：协议=用户每次
    主动要求更新时才执行；用户发持仓截图即以截图为准，未发截图即表示该期间无成交。
    定时任务/后台不得自行推进——必须由用户在会话中当次发起。
- 模型建议不等于成交；未经用户确认不得写成已下单、已成交或已持仓。
- 不接券商、不自动下单，不新增任何让语言模型直接持有交易凭据的能力。
trade_record 落账路径,协议=你发成交截图+我展示完整命令+你确认后我执行。
  - `data/holdings.json` 的 **`stop` 与 `conditional_orders` 字段**(2026-08-21 追加授权)：协议=智能体展示完整将写入内容→用户确认→逐字写入并回报校验;stop 只按 stop_policy 制度区间取值,条件单只登记用户提供的券商事实,不得自行推断到期日或是否存在。

## 研究与生产不变量

- 生产 composite 固定为 `EP + BP + IVOL（负向）`，并遵循行业与市值中性；不得为让测试通过而增删因子、改权重或改门槛。
- 新因子必须具备文献或理论先验，先登记实验，再经过 PIT、成本、LOYO、市场状态、多头腿及增量/容量复核；机械过门不等于自动入分。
- 所有决策只使用 as-of 当时可见信息。公告、更正、fact-check 和财务数据不得前视；必须保留已知 PIT 覆盖缺口并如实说明。
- 核心 EOD 数据为 `daily`、`adj_factor`、`daily_basic`、`stk_limit`；缺失或退化 schema 必须 fail-loud，未知不得解释为安全、零事件或可正常生产。
- 腾讯盘中行情只作盘中辅助，绝不替代或写入 Tushare EOD 研究缓存。
- 最新完整机器 decisions snapshot 是机器状态真相源。外部证据只能补充或否决，绝不能把 WAIT 提升为 BUY。
- fact-check 只有否决权；不得把 D10 外股票提升为正式 BUY。允许没有 BUY，不得为凑名单降低门槛。
- 缺少正式机器状态、结构化买入价、有效股数或条件单核验时保持 WAIT，不得用现价、“附近”或自由文本补造交易参数。

## 编码与测试

- 优先复用现有函数、错误类型、数据契约和测试模式；import 阶段不得产生网络、文件或 stdout 副作用。
- 先运行与改动直接相关、无网络的 pytest；跨层改动再运行完整测试。
- 标准命令：`E:\zwyq\.venv\Scripts\python.exe -m pytest <tests...>`。
- 项目当前没有 Ruff、Black、mypy 或 Pyright 作为仓库门禁，不得假称这些检查已通过。
- 测试失败必须如实报告；不得删除测试、放宽断言或改变生产口径来掩盖失败。
- 最终报告必须列出修改文件、行为变化、实际测试命令、结果和遗留风险。

## 代码理解工具

- CodeGraph 可用于符号、调用链和影响面分析；简单且已知路径的定位不必强制调用。CodeGraph 不能替代源码阅读、金融领域审计或测试。
- Understand Anything 默认禁用。只有用户明确要求架构 dashboard、onboarding 或全仓语义图，且先审查代码外发和忽略范围后，才可按次启用。 
- `data/trading_policy.json`
- `data/holdings.json` 的 **`as_of` 字段**(2026-08-19 追加授权,仅限`scripts.holdings_confirm` 这一条推进路径,只改日期不动持仓/现金)：协议=用户每次主动要求更新时才执行；用户发持仓截图即以截图为准，未发截图即表示该期间无成交。定时任务/后台不得自行推进——必须由用户在会话中当次发起。