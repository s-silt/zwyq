# A 股量化与实盘风控项目协作规则

## 职责边界

- 主智能体负责调查、需求澄清、技术设计、任务合同、最终审核、测试与修复。
- OpenHarness 仅按主智能体已经确定的设计实施一个边界明确的代码任务；不得自行改变架构、研究口径或交易政策。
- 同一时间只允许一个编码子助理修改当前工作树。OpenHarness 内不得递归启动其他 agent。
- 子助理完成不等于任务完成；主智能体必须查看实际 diff，不得只采信子助理摘要。

## 环境与工作区

- 项目根目录固定为 `E:\zwyq`。
- Python 固定使用 `E:\zwyq\.venv\Scripts\python.exe`，要求 Python 3.12+；不要调用系统 Python或创建第二套环境。
- 开工前检查 `git status --short --branch`，修改目标文件前检查其现有 diff。
- 当前工作树可能包含用户或其他进程的未提交改动。禁止 `git reset`、`restore`、`checkout --`、`clean`、`stash`，禁止覆盖、丢弃或顺手整理既有改动。
- 主智能体依据用户请求建立简短任务合同，列明目标、修改路径与验证方法；可说明原因后补充完成同一目标必需的普通源码、测试路径，不得扩大目标或纳入受保护数据。子助理只修改主智能体已审核合同列明的路径。
- 修改已有改动的文件前，主智能体必须理解现有 diff 并保护既有改动；交给子助理时须明确授权增量修改，主智能体自行执行无须自我审批。
- 禁止无关重构、全仓格式化、整文件换行转换以及修改任务外文件。
- 未经用户明确要求，不得 commit、push、创建 PR、merge 或切换分支。授权按终点区分：提交止于本地 commit，推送止于 push，创建 PR 止于 PR；合并须明确授权。已明确授权的完整流水线可连续执行，不重复确认。
- 默认在指定项目目录和当前分支工作，普通本地修改不因 Skills 强制要求隔离而创建 worktree、切分支、提交配置或安装另一套环境。

## 自主执行与任务完成

- 用户明确要求实施且范围清楚时，简述方案后完成实现、排错和验证；仅要求审阅、建议或计划时，止于对应交付物。
- 仅在缺少必需信息、需要扩大范围或触及下述专门审批时等待用户；继续不依赖该问题的工作。已确认且未实质变化的普通方案不重复审批。
- 测试失败先在授权范围内定位、修复和复测；不能解决时报告原因、证据和所缺条件，不得冒称完成。验证后文件、输入及相关环境未变时可复用结果，说明命令和覆盖范围。
- 账户和交易政策仍执行下述当次具体确认；普通任务自主执行不豁免这些要求。

## 密钥、账户与人工状态

- 禁止读取、打印、记录或提交 `.env*`、Tushare token、API key、Credential Manager 内容、认证 header 和其他秘密。
- `data/holdings.json`、`data/profile.json`、`data/trigger_bands.json`、`data/trade_journal.json` 默认禁止智能体修改，也不得完整输出。仅允许下表明确列出的既有例外；`profile.json`、`trigger_bands.json` 没有写入例外。
- “完整将写入内容”指本次拟变更字段的完整值或精确补丁，不指整个受保护账户文件。先展示具体内容或规定的完整命令，收到用户对该内容的当次明确确认后才执行；内容变化须重新展示确认，旧确认不得沿用，超时和沉默不算确认。

| 例外 | 范围与入口 | 当次确认要求 |
|---|---|---|
| 决策代笔（2026-08-15） | `data/factcheck_overrides.json`、`data/trading_policy.json`；仅逐字写入用户确认内容 | 先展示完整拟写内容，确认后写入并回读校验；verdict/参数判断权始终在用户 |
| 日期确认（2026-08-19） | 仅经 `scripts.holdings_confirm` 修改 `data/holdings.json` 的 `as_of`，不改持仓/现金 | 用户在当前会话主动要求更新后，展示目标日期和完整命令，再等该次确认；有持仓截图以截图为准，该主动请求未附截图表示该期间无成交。定时任务/后台不得推进 |
| 止损与条件单（2026-08-21） | `data/holdings.json` 的 `stop` 与 `conditional_orders` 字段 | 展示全部拟变更字段值，确认后逐字写入并校验；stop 只按 stop_policy 制度区间取值，条件单只登记用户提供的券商事实，不推断到期日或是否存在 |
| 人工成交落账（既有 trade_record 协议） | 仅经 `scripts.trade_record` 的买入、整仓卖出、部分减仓入口；买入更新 `holdings.json` 的本笔新仓及现金，卖出/减仓更新本笔持仓、现金及 `trade_journal.json` 的本笔流水，整仓卖出含 `closed` 记录；不允许手工绕过入口 | 用户发成交截图 → 展示完整命令及本笔影响字段 → 用户确认 → 执行并回报校验。只执行截图支持且确认的本笔事实，不调用真实账户试跑；失败不得盲目重跑或手工修账 |

- 日期推进仍仅走上表 `holdings_confirm` 路径；不得因 `trade_record` 存在 `--advance-as-of` 参数自行启用另一条日期推进路径。程序可实现的能力不等于用户授权。
- 模型建议不等于成交；未经用户确认不得写成已下单、已成交或已持仓。
- 不接券商、不自动下单，不新增任何让语言模型直接持有交易凭据的能力。

## 研究与生产不变量

- 生产 composite 固定为 `EP + BP + IVOL（负向）`，并遵循行业与市值中性；不得为让测试通过而增删因子、改权重或改门槛。
- 新因子必须具备文献或理论先验，先登记实验，再经过 PIT、成本、LOYO、市场状态、多头腿及增量/容量复核；机械过门不等于自动入分。
- 所有决策只使用 as-of 当时可见信息。公告、更正、fact-check 和财务数据不得前视；必须保留已知 PIT 覆盖缺口并如实说明。
- 核心 EOD 数据为 `daily`、`adj_factor`、`daily_basic`、`stk_limit`；缺失或退化 schema 必须 fail-loud，未知不得解释为安全、零事件或可正常生产。
- 腾讯盘中行情只作盘中辅助，绝不替代或写入 Tushare EOD 研究缓存。
- 最新完整机器 decisions snapshot 是机器状态真相源。外部证据只能补充或否决，绝不能把 WAIT 提升为 BUY。
- fact-check 只有否决权；不得把 D10 外股票提升为正式 BUY。允许没有 BUY，不得为凑名单降低门槛。
- 缺少正式机器状态、结构化买入价、有效股数或条件单核验时保持 WAIT，不得用现价、“附近”或自由文本补造交易参数。

## 编码与测试

- 优先复用现有函数、错误类型、数据契约和测试模式；import 阶段不得产生网络、文件或 stdout 副作用。
- 先运行与改动直接相关、无网络的 pytest；跨层改动再运行完整测试。
- 标准命令：`E:\zwyq\.venv\Scripts\python.exe -m pytest <tests...>`。
- 项目当前没有 Ruff、Black、mypy 或 Pyright 作为仓库门禁，不得假称这些检查已通过。
- 测试失败必须如实报告；不得删除测试、放宽断言或改变生产口径来掩盖失败。
- 最终报告必须列出修改文件、行为变化、实际测试命令、结果和遗留风险。

## 代码理解工具

- 存在 `.codegraph/` 时优先用 CodeGraph 分析未知符号、调用链和影响面；已知文件读取和简单精确定位可直接使用文件工具。工具不可用、失败或未覆盖所需内容时继续常规检索，不自动安装或索引。CodeGraph 不能替代金融领域审计或测试。
- Understand Anything 默认禁用。只有用户明确要求架构 dashboard、onboarding 或全仓语义图，且先审查代码外发和忽略范围后，才可按次启用。
